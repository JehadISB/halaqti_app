import 'package:flutter/material.dart';

class StudentsViewBody extends StatelessWidget {
  const StudentsViewBody({
    super.key,
  });

  @override
  Widget build(BuildContext context) {
    return Column(
      children: [DropdownButton(items: [], onChanged: null)],
    );
  }
}
